<button
        type="button"
        data-bs-target="{{ target }}"
        data-bs-slide-to="{{ slide_to }}"
        {% if is_active %}
            class="active"
        {% endif %}
></button>